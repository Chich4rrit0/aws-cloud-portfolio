# The layer modules are added incrementally. This environment intentionally has no
# resources or module calls until each equivalent CloudFormation layer is reviewed.
# With no backend block, Terraform uses its default local state backend; state files
# are ignored by Git and will never be committed.
